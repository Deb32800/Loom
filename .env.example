# Copy to .env and adjust as needed for local development.
# docker-compose.yml's postgres/redis services match these defaults.
DATABASE_URL=postgresql+asyncpg://loom:loom@localhost:5432/loom
REDIS_URL=redis://localhost:6379/0
